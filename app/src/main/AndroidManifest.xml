<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TK"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 用户主界面Activity -->
        <activity
            android:name=".UserMainActivity"
            android:label="用户主界面"
            android:parentActivityName=".MainActivity"
            android:exported="false">
        </activity>

        <!-- 用户me界面Activity -->
        <activity
            android:name=".UserMainMeActivity"
            android:label="用户ME界面"
            android:parentActivityName=".UserMainActivity"
            android:exported="false">
        </activity>

        <!-- 用户message界面Activity -->
        <activity
            android:name=".UserMainMessageActivity"
            android:label="用户message界面"
            android:parentActivityName=".UserMainActivity"
            android:exported="false">
        </activity>

        <!-- 用户new界面Activity -->
        <activity
            android:name=".UserMainNewActivity"
            android:label="用户new界面"
            android:parentActivityName=".UserMainActivity"
            android:exported="false">
        </activity>

        <!-- 用户Login界面Activity -->
        <activity
            android:name=".SignupActivity"
            android:label="用户login界面"
            android:parentActivityName=".UserMainActivity"
            android:exported="false">
        </activity>

    </application>

</manifest>