<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.TK"
        tools:targetApi="31">
        <activity
            android:name=".backActivity.EmailActivity"
            android:exported="false" />
        <activity
            android:name=".backActivity.AddFriendActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignLog.Sea_deluser_Activity"
            android:exported="false"
            android:label="用户ME界面"
            android:parentActivityName=".backActivity.starActivity" />
        <activity
            android:name=".SignLog.choiseUser_Activity"
            android:exported="false"
            android:label="用户choise界面"
            android:parentActivityName=".backActivity.starActivity" />
        <activity
            android:name=".backActivity.Insertuser_Activity"
            android:exported="false"
            android:label="用户ME界面"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".backActivity.starActivity"
            android:exported="false"
            android:label="用户ME界面"
            android:parentActivityName=".MainActivity" /> <!-- 用户主界面Activity -->
        <activity
            android:name=".activity.UserMainActivity"
            android:exported="false"
            android:label="用户主界面"
            android:parentActivityName=".MainActivity" /> <!-- 用户me界面Activity -->
        <activity
            android:name=".activity.UserMainMeActivity"
            android:exported="false"
            android:label="用户ME界面"
            android:parentActivityName=".activity.UserMainActivity" /> <!-- 用户message界面Activity -->
        <activity
            android:name=".activity.UserMainMessageActivity"
            android:exported="false"
            android:label="用户message界面"
            android:parentActivityName=".activity.UserMainActivity" /> <!-- 用户new界面Activity -->
        <activity
            android:name=".activity.UserMainNewActivity"
            android:exported="false"
            android:label="用户new界面"
            android:parentActivityName=".activity.UserMainActivity" /> <!-- 用户Login界面Activity -->
        <activity
            android:name=".SignLog.SignupActivity"
            android:exported="false"
            android:label="用户signup界面"
            android:parentActivityName=".SignLog.LoginActivity" />
        <activity
            android:name=".SignLog.LoginActivity"
            android:exported="false"
            android:label="用户login界面"
            android:parentActivityName=".activity.UserMainMeActivity" />
    </application>

</manifest>